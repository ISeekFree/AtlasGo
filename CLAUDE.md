# Claude Code Instructions

Use [AGENTS.md](AGENTS.md) as the canonical repository guide and read [Architecture.md](Architecture.md) before cross-module work.

In particular, preserve optional module boundaries, programmatic construction alongside YAML loading, MongoDB entity-level compound indexes, Redis pool/timeout mappings, and multiple lazy named gRPC channels. Run `./build.sh` after broad changes.
